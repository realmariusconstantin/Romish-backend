// testAcceptPhase.js removed
// This script previously created simulated users and auto-joined them to the queue.
// Per the repository policy and user request to remove all simulated players, this
// file has been disabled. To restore functionality, retrieve the original from
// version control or re-add a safe, opt-in test harness under a dedicated test-only
// folder that cannot be run accidentally in production.

console.log('testAcceptPhase.js has been disabled to remove simulated player creation.');
process.exit(0);
